1. how to convert the string date column to proper date time type in pandas? (option:b)
   a)to_date()  b)to_datetime() c) to_time() d)to_strip_datetime()

2. How to fill nan values with the data present previous k records values? (option: c)
   a)fillna()  b) fillnan() c) rolling() d) fill_k_nan()

3. how do you convert the datatype of a column? (option: c)
   a)convert() b)changedtype() c)astype() d)transform()

4. how do you count the rows that are having nan in particular column? (option: a)
   a) isnull().sum() b) nulls().count() c) nan().sum() d) isnull().all()

5. how do you merge two dataframes vertically assume if they are having same column names? (option b:)
   a) pd.join()  b)pd.concat() c) pd.merge() d)pd.combine() 

6. which of the below function do you use to create new column by using the existing columns? option b(using eval, ex: I have name,age columns in sample df, 
   a) pd.create() b) pd.eval() c)pd.append() d) pd.add_column()				now I want to create new column that will have twice the value of age)
											code: pd.eval("double_age = sample.age * 2", target=sample)	